import { SelectDropdownOptionPipe } from './select-dropdown-option.pipe';

describe('SelectDropdownOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectDropdownOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
